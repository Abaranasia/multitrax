# CSS Conventions

## Core Rules

- **Always use CSS files co-located with the component** — never write styles outside the component's folder.
- **No inline styles in JSX** — use CSS classes instead.  
- **BEM naming methodology** — see below.

---